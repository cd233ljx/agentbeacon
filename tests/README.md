# 测试

使用 Node.js 内置 `node:test`，运行：

```bash
npm test
```

当前覆盖 Herdr 被动探针与 protocol 20、历史事件 reconciliation、订阅断线重连、多会话聚合、sender 配置/序列/心跳/合并重试、v1 协议与 Receiver、独立 Demo，以及本地模拟 WLED。模拟结果不替代 Android、Windows 或真实 WLED 验收。
