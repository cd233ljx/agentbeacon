# 共享业务逻辑（待实现）

正常优先级 blocked > working > unknown > done > idle；done 绿色常亮，不自动超时。已完成会话关闭后清除其完成状态，按剩余有效会话重新聚合；无其他有效任务则 idle 熄灯。通信异常仍为 unknown（D-004、D-007）。

区分业务状态与通信健康；空闲不是采集失败。正式定义见 [协议与状态规则](../docs/protocol.md)。

实现按会话生命周期更新的聚合和正式协议校验；业务测试覆盖状态切换、删除、过期及乱序。
