# AgentBeacon 开发入口

默认用简体中文沟通。本项目的软件开发由 Agent 执行，用户负责产品决策、设备操作和最终验收。

## 每次开始

1. 读取 README.md、docs/proposal.md、docs/project-management.md。
2. 读取 docs/tasks.md、docs/decisions.md、docs/handoff.md，确认下一项可执行任务。
3. 检查 git status，保留用户及其他任务的修改。仅在理解已有变更后编辑。
4. 按需读取目标目录说明；若仓库存在 .codegraph/，先使用 CodeGraph 理解代码，否则不自行建立索引。

## 工作规则

- 以当前方案为准，docs/proposal-original.md 是历史原文，不作为实现依据。
- 用户已授权任务范围内的可逆实现、修复、测试和文档维护直接完成，不逐步请求确认。
- 未确认的产品语义查 docs/decisions.md；只暂停依赖该决定的部分，继续独立工作，不把建议写成已批准要求。
- 每项任务先声明范围和验收条件，在 docs/tasks.md 更新负责人和状态，完成后记录实际验证证据。
- 优先 Node.js 原生 ESM 和内置工具；新增依赖应说明必要性，维护锁文件，不全局安装。
- 核实本机 Herdr 版本及其官方接口，不编造事件字段、插件能力、状态来源或 CLI 参数。
- Receiver 支持 Android Termux 和 Windows；核心逻辑不依赖 bash、systemd 或 Linux 专有路径。
- 不监控 Herdr 外部 Agent，不读取 agent 凭据、提示词或终端正文来替代状态 API。
- 开发默认回环监听；私网联调显式配置具体 Tailscale IP。遵循上级服务器操作红线，不自行开放公网或 0.0.0.0。
- 没有设备访问能力时提供准确的用户操作步骤和预期结果，保留待验证状态，不声称已完成真机联调。
- 业务测试覆盖边界和故障，不能用语法检查或模拟器通过替代真机验收。
- 不默认创建远程仓库、推送或发布。不要为本项目改动全局认证、网络或现有服务。

## 每次结束

更新任务状态、决策记录和交接；说明改动、验证结果、限制和下一步。交接必须足以让新 Agent 不读取聊天也能继续。
文档纯修改做链接/一致性检查即可；业务修改运行对应测试与 npm run check。
