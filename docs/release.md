# 开源发布准备

首版开发和用户验收已完成。本轮整理公开文档并加入 Apache-2.0；维护者已授权提交和推送，选择保留 Git 历史。不修改 GitHub 可见性。

## 当前准备结果

- README 面向使用者，提供显式回环 Demo 和真实部署入口。
- 许可证为 LICENSE（Apache-2.0），版权说明 NOTICE；源码和独立 Receiver 包均携带许可信息。
- 文档里的私人部署地址、账号、主机指纹和路径已脱敏；历史验证文件标明当时状态。
- 当前未验证的平台与故障恢复限制集中在 limitations.md；首版不再以未完成开发看板呈现。

## 历史与本地配置

默认 URL 已改为回环；维护者授权用环境变量保留答辩目标。本机 .env.local 已设置既有地址并被 Git 忽略，npm run sender/demo 自动加载；真实 receiver/config.json、server/config.json（如存在）和手机部署配置不修改。公开 .env.example 仅包含回环示例。

已有 Git 历史记录过内网地址、部署路径和主机指纹。维护者于 2026-09-13 确认这些主要是内网部署信息，接受保留历史并正常推送；不重写或强推。公开仓库时旧版本仍可访问，当前文档脱敏不会擦除历史。

不要发布 config.local.h、SSH 密钥、.env、日志或含 Wi-Fi 凭据的固件镜像。源码默认配置为禁用灯光输出；发布固件二进制需要单独确认配置不含真实凭据。许可证不自动覆盖第三方工具链和依赖，各自遵循上游许可。

## 本地检查命令

```bash
npm test
npm run check
npm run package:receiver
```

检查压缩包仅含白名单文件、LICENSE/NOTICE，且校验文件匹配。保留 package.json 的 private:true 是防止误发布 npm，不影响 GitHub 源码开源。

本轮实测：51 项 Node 测试、check、文档链接、当前已知私人部署标识扫描、环境文件加载、包白名单及校验均通过。另对当时全部 14 个提交可达文件扫描常见私钥/令牌标记和本地配置路径，未命中；命中 60 个含已知私人部署标识的历史 blob。该定向检查不是全类型秘密检测保证。历史保持原样。

## 发布时由维护者执行

历史策略已确认，本轮按正常提交推送；GitHub 可见性变更及可选 Release 仍是后续独立操作。需要时启用 GitHub 私密漏洞报告；SECURITY.md 不假设它已经启用。
