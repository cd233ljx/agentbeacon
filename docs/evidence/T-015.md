# T-015：真实输出日志及手机部署验证

最新运行状态：用户要求改为手机前台启动；已核对 PID 20548 的命令与 cwd 后发送 SIGTERM，确认进程退出并清除 receiver.pid。后台 Receiver 已停止，手机可执行 `cd ~/agentbeacon/releases/receiver-et6Qrz && npm start`。下文后台启动记录为本轮验证历史。


日期：2026-09-13；负责人：Codex。

## 实现与本机验证

真实输出完整读取 HTTP 响应并确认成功后打印 `WLED 已应用 state=working preset=2`。沿用输出层 generation 去重，不在心跳接收处打印；失败重试仍保留警告。

`node --test tests/wled-output.test.mjs`：3/3 通过，验证成功映射与去重、503 有界失败不打印成功、下一次心跳恢复只打印一次、新状态取代旧重试。`npm run check` 通过。

## 手机现场与部署

SSH 首次拒绝连接，用户恢复 sshd 后成功。检查时没有运行中的 Receiver，无需停止旧进程。Node v26.3.1；目录 `/data/data/com.termux/files/home/agentbeacon/releases/receiver-et6Qrz`。

仅替换 `receiver/wled-output.mjs`，旧文件保存在 `receiver/wled-output.mjs.pre-T015`。通过标准输入执行 `node --input-type=module --check` 成功（首次直接检查 .T015 暂存扩展名被 Node 拒绝，改用标准输入后通过）。本机与手机新文件 SHA-256 均为 `c75f5491195fba022d7abd0bd0fea17b72e894491bdb98a4a244cb393949902b`。

配置未修改：监听 100.91.207.103:8787，dryRun=false，wledBaseUrl=http://10.40.220.147，超时 15000ms，demoEnabled=false。部署前后 config.json SHA-256 均为 `fe3a951e6c34d1c374ca98cc026bb99132a2572643b106a27293efeb085aaaf6`。

使用 nohup 后台运行 `node scripts/run-receiver.mjs --config receiver/config.json`，追加 receiver.log。已核对 PID 20548 的 cwd 和命令后更新 receiver.pid；该 PID 仅为此次现场值，未来停止前须重新核对。

## 真实网络验证

服务器向手机 `/v1/state` 发送独立 UUID、递增 sequence 的合法快照，设备通过真实 HTTP 输出：

- 启动：unknown/preset 5 成功日志。
- sequence 1～6：idle、working、blocked、done、unknown、working，均返回 applied，日志分别对应 preset 1、2、3、4、5、2。
- sequence 7～9：间隔 5 秒发送 working，均返回 refreshed，日志没有增加；health 为 working、timed_out=false、dry_run=false。
- 停止心跳后等待 16.5 秒：health 为 unknown、timed_out=true；增加一次 unknown/preset 5 成功日志。
- sequence 10：done 返回 applied，health 恢复 done、timed_out=false；增加一次 done/preset 4 成功日志。

本次启动至恢复共 9 条成功日志，无失败日志。检查结束后没有持续发送器，约 15～16 秒后会再次正常超时 unknown。

## 限制及回滚

用户此前已确认 Demo 真灯五态和超时恢复的视觉效果；本轮 Agent 验证的是手机实际部署、真实设备 HTTP 成功日志和去重，不声称独立观察灯光。真实 Herdr 多 Agent 和长期后台仍待验收。未推送 GitHub。

回滚：核对并停止当前 Receiver 后，将同目录 `receiver/wled-output.mjs.pre-T015` 复制回 `receiver/wled-output.mjs`，按上述命令重启，保留 config.json。查看日志可在手机运行 `tail -f ~/agentbeacon/releases/receiver-et6Qrz/receiver.log`；已有后台服务时不要再运行 npm start 占用同一端口。

最终收口：用户已确认全部完成并授权提交；此前“未提交/待观察”为当时验证现场。最终验收见 [T-010](T-010.md)，本轮全套 Node 测试 49/49 通过。
