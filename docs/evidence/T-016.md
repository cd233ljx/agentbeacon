# T-016：Sender 日志

> 历史验证记录：地址、用户名和部署路径已替换为示例；日期、测试结果及限制保留。记录描述当时状态，当前首版验收见 [T-010](T-010.md)。


2026-09-13，Codex。用户反馈真实 Herdr 联调“成了”，并指出 Sender 缺少日志；具体多 Agent、blocked/done 及断线场景未逐项回传，T-010 不据此整体完成。

首次 Receiver 确认、state/cause 变化或发送故障恢复后记录 state/cause/sequence/disposition；相同状态的正常心跳安静。stale/retired_instance 记录未采用提示，重复处置不刷屏；不把 HTTP 200 误当设备成功。没有改变协议、发送频率或 Demo 优先规则。

验证：node --test tests/snapshot-sender.test.mjs tests/sender-app.test.mjs，6/6 通过；覆盖确认/心跳去重、503 后恢复、duplicate、退休及过期快照警告、状态/原因变化，及已有 Herdr 模拟→Receiver 集成。npm run check、git diff --check 和修改文档本地链接检查通过。

未重启用户 Sender，未操作手机；用户在服务器 Ctrl+C 后 npm run sender 加载新代码，手机无需重启。真实日志待用户观察。未提交、未推送。

最终收口：用户已确认全部完成并授权提交；此前“未提交/待观察”为当时验证现场。最终验收见 [T-010](T-010.md)，本轮全套 Node 测试 49/49 通过。
